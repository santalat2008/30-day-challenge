MOD=998244353
n,k=map(int,input().split())
p=list(map(int,input().split()))

pos=[0]*(n+1)
for i,x in enumerate(p):
    pos[x]=i

topk=sorted(pos[n-k+1:])

res=sum(range(n,n-k,-1))
ways=1
for i in range(1,k):
    ways=(ways*(topk[i]-topk[i-1]))%MOD

print(res,ways)
