t = int(input())
for _ in range(t):
    s = list(input().strip())
    n = len(s)
    res = []
    for i in range(1, n):
        if s[i] != '0':
            s[i] = str(int(s[i]) - 1)
            s[i-1], s[i] = s[i], s[i-1]
    s.sort()
    print(''.join(s[::-1]))
