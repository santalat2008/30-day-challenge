for _ in range(int(input())):
    n, a, b, c = map(int,input().split())
    x = list(map(int,input().split()))
    t = float('inf')
    for i in x:
        t = min(t, abs(a - i) + c + abs(b - i))
    print(t)
