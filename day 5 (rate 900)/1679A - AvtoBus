for _ in range(int(input())):
    n = int(input())
    if n % 4 != 0 and n % 6 != 0 and n < 6:
        print(-1)
    else:
        mn = (n + 5) // 6
        mx = n // 4
        if mn * 4 > n or mx * 6 < n:
            print(-1)
        else:
            print(mn, mx)
