def f(x):
    h, m = map(int, x.split(':'))
    return h * 5 + m / 12

for _ in range(int(input())):
    t, a, b = input().split()
    x = f(t)
    l = min(f(a), f(b))
    r = max(f(a), f(b))
    print("YES" if l < x < r else "NO")
