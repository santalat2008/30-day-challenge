t = int(input())
for _ in range(t):
    n = int(input())
    s = input()
    seen = set()
    ok = False
    for i in range(n - 1):
        sub = s[i:i+2]
        if sub in seen:
            ok = True
            break
        seen.add(sub)
    print("YES" if ok else "NO")
