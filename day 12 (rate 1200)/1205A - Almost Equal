n = int(input())
if n % 2 == 0:
    print("NO")
else:
    print("YES")
    a = [0] * (2 * n)
    for i in range(n):
        if i % 2 == 0:
            a[i] = 2 * i + 1
            a[i + n] = 2 * i + 2
        else:
            a[i] = 2 * i + 2
            a[i + n] = 2 * i + 1
    print(*a)
