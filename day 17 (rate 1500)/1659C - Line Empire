t = int(input())
for _ in range(t):
    n, a, b = map(int, input().split())
    x = list(map(int, input().split()))
    ans = 0
    pos = 0
    for i in range(n):
        ans += b * (x[i] - pos)
        if i < n - 1:
            ans += min(a, b) * (x[i+1] - x[i])
        pos = x[i]
    print(ans)
