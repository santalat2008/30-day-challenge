t = int(input())
for _ in range(t):
    n, x = map(int, input().split())
    a = sorted(map(int, input().split()), reverse=True)
    s = 0
    ans = 0
    for i in range(n):
        s += a[i]
        if s >= (i + 1) * x:
            ans = i + 1
    print(ans)
