import math
s1=input().strip()
s2=input().strip()
g=math.gcd(len(s1),len(s2))
ans=0
for l in range(1,g+1):
    if g%l==0:
        p=s1[:l]
        if p*(len(s1)//l)==s1 and p*(len(s2)//l)==s2:
            ans+=1
print(ans)
