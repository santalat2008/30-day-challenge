t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
    if n == 3 and a[1] % 2 == 1:
        print(-1)
        continue
    ok = any(x > 1 for x in a[1:-1])
    if not ok:
        print(-1)
        continue
    print(sum((x + 1) // 2 for x in a[1:-1]))
