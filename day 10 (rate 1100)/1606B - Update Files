def f(n, k):
    if n == 1:
        return 0
    u, h = 1, 0
    while u < n:
        c = min(u, k)
        u += c
        h += 1
    return h

t = int(input())
for _ in range(t):
    n, k = map(int, input().split())
    print(f(n, k))
