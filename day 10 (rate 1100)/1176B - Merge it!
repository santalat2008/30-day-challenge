t = int(input())
for _ in range(t):
    n = int(input())
    a = list(map(int, input().split()))
    r = [0, 0, 0]
    for x in a:
        r[x % 3] += 1
    ans = r[0]
    m = min(r[1], r[2])
    ans += m
    r[1] -= m
    r[2] -= m
    ans += (r[1] // 3) + (r[2] // 3)
    print(ans)
