def d(a, b):
    return abs(a[0] - b[0]) + abs(a[1] - b[1])

t = int(input())
for _ in range(t):
    a = tuple(map(int, input().split()))
    b = tuple(map(int, input().split()))
    c = tuple(map(int, input().split()))
    ans = (d(a, b) + d(a, c) - d(b, c)) // 2 + 1
    print(ans)
