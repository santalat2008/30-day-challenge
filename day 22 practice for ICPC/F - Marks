n, m = map(int, input().split())
g = [input().strip() for _ in range(n)]
best = [0] * n
for j in range(m):
    mx = max(g[i][j] for i in range(n))
    for i in range(n):
        if g[i][j] == mx:
            best[i] = 1
print(sum(best))
