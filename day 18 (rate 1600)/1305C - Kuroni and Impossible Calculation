n, m = map(int, input().split())
a = list(map(int, input().split()))
if n > m:
    print(0)
else:
    a.sort()
    ans = 1
    for i in range(n):
        for j in range(i+1, n):
            ans = (ans * (a[j] - a[i])) % m
    print(ans)
